<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- <PublishAot>true</PublishAot>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
    <InvariantGlobalization>true</InvariantGlobalization>
    <StripSymbols>true</StripSymbols>
    <IlcGenerateCompleteTypeMetadata>false</IlcGenerateCompleteTypeMetadata>
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>link</TrimMode> -->
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\Asmo\Asmo.csproj" />
    </ItemGroup>

  <ItemGroup>
    <!-- Use engine's Raylib-cs; remove OpenTK from runtime -->
    <PackageReference Include="Raylib-cs" Version="5.0.0" />
    <PackageReference Include="Evergine.Bindings.OpenXR" Version="2025.9.1.27" />
  </ItemGroup>

  <!-- we included these in the binary, not removing this yet. might need it. -->
  <!-- <ItemGroup>
    <None Update="Window\Shaders\shader.frag">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
    <None Update="Window\Shaders\shader.vert">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
    </None>
  </ItemGroup> -->
</Project>
